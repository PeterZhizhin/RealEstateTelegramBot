# Uncomment if need to run mongo
# docker run -v /root/mongodb/:/data/db -p 127.0.0.1:27017:27017 --name mongo-container -d mongo:latest --dbpath=/data/db
docker run --name telegram_realty --link mongo-container:localhost -v $(pwd):/source -d telegram_realty
